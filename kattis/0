import sys

input = sys.stdin

for line in input:
    num = (line.strip()).split()
    num = map(int, num)

for line in input:
    max = int(line)
    num = (line.strip()).split()
    num = map(int, num)

